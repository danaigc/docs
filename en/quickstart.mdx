---
title: "Quickstart"
description: "Generate your first image in under 5 minutes"
---

## Prerequisites

Before you begin, you'll need:

1. An Egg API account - [Sign up here](https://eggapi.ai)
2. An API key from your [dashboard](https://eggapi.ai/dashboard)

## Step 1: Get Your API Key

<Steps>
  <Step title="Sign up or log in">
    Go to [eggapi.ai](https://eggapi.ai) and create an account or log in.
  </Step>
  <Step title="Navigate to API Keys">
    In your dashboard, go to the API Keys section.
  </Step>
  <Step title="Create a new key">
    Click "Create API Key" and give it a name. Copy and save your key securely - you won't be able to see it again.
  </Step>
</Steps>

<Warning>
  Keep your API key secure. Never expose it in client-side code or public repositories.
</Warning>

## Step 2: Make Your First Request

### Generate an Image (Synchronous)

Image models like `nanobanana` return results immediately:

<CodeGroup>

```bash cURL
curl -X POST https://api.eggapi.ai/v1/generate \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "nanobanana-pro",
    "prompt": "A cute robot holding a coffee cup, digital art style",
    "width": 1024,
    "height": 1024
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.eggapi.ai/v1/generate",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "nanobanana-pro",
        "prompt": "A cute robot holding a coffee cup, digital art style",
        "width": 1024,
        "height": 1024
    }
)

result = response.json()
print(f"Image URL: {result['data']['urls'][0]}")
print(f"Cost: {result['data']['cost']}")
```

```javascript JavaScript
const response = await fetch("https://api.eggapi.ai/v1/generate", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "nanobanana-pro",
    prompt: "A cute robot holding a coffee cup, digital art style",
    width: 1024,
    height: 1024
  })
});

const result = await response.json();
console.log("Image URL:", result.data.urls[0]);
console.log("Cost:", result.data.cost);
```

</CodeGroup>

**Response:**

```json
{
  "data": {
    "id": "gen_abc123def456",
    "model": "nanobanana-pro",
    "urls": ["https://cdn.eggapi.ai/outputs/abc123.png"],
    "cost": "$0.0160"
  },
  "error": null
}
```

### Generate a Video (Asynchronous)

Video models like `veo3.1` are asynchronous - you submit a request and poll for results:

<CodeGroup>

```bash cURL
# Step 1: Submit the task
curl -X POST https://api.eggapi.ai/v1/generate \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "veo3.1",
    "prompt": "A cat playing with a ball of yarn",
    "duration": 5,
    "aspect_ratio": "16:9"
  }'

# Response: {"data": {"id": "gen_xyz789", "status": "pending", ...}}

# Step 2: Poll for results
curl https://api.eggapi.ai/v1/tasks/gen_xyz789 \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```python Python
import requests
import time

# Step 1: Submit the task
response = requests.post(
    "https://api.eggapi.ai/v1/generate",
    headers={"Authorization": "Bearer YOUR_API_KEY"},
    json={
        "model": "veo3.1",
        "prompt": "A cat playing with a ball of yarn",
        "duration": 5,
        "aspect_ratio": "16:9"
    }
)

task = response.json()["data"]
task_id = task["id"]
print(f"Task created: {task_id}")

# Step 2: Poll for results
while True:
    status_response = requests.get(
        f"https://api.eggapi.ai/v1/tasks/{task_id}",
        headers={"Authorization": "Bearer YOUR_API_KEY"}
    )
    task = status_response.json()["data"]

    if task["status"] == "completed":
        print(f"Video URL: {task['output']['url']}")
        print(f"Duration: {task['output']['duration_seconds']}s")
        print(f"Cost: {task['cost']}")
        break
    elif task["status"] == "failed":
        print(f"Error: {task['error']}")
        break
    else:
        print(f"Status: {task['status']}...")
        time.sleep(5)
```

</CodeGroup>

## Step 3: Check Your Usage

View your API usage and costs:

```bash
curl https://api.eggapi.ai/v1/usage/summary \
  -H "Authorization: Bearer YOUR_API_KEY"
```

**Response:**

```json
{
  "data": [
    {
      "model": "nanobanana-pro",
      "total_units": 10,
      "total_cost": "$0.1600",
      "request_count": 10
    },
    {
      "model": "veo3.1",
      "total_units": 25,
      "total_cost": "$2.0000",
      "request_count": 5
    }
  ],
  "error": null
}
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/guides/authentication">
    Learn more about API authentication
  </Card>
  <Card title="Models & Pricing" icon="cube" href="/guides/models">
    Explore available models and their pricing
  </Card>
  <Card title="Async Tasks" icon="clock" href="/guides/async-tasks">
    Learn about async task handling
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/introduction">
    Full API documentation
  </Card>
</CardGroup>
